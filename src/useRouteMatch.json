{
	"import useRouteMatch": {
		"prefix": ["imurm"],
		"body": ["import { useRouteMatch } from 'react-router-dom'"],
		"description": "import `useRouteMatch`.\n"
	},
	"React Router useRouteMatch": {
		"prefix": ["urm", "useRouteMatch"],
		"body": [
			"const match = useRouteMatch()",
			"",
			"// build nested routes and links using match.url and `match.path`",
			"<Link to={`${match.url}/me`}>My Profile</Link>",
			"<Route path={`${match.path}/me`}><OwnUserProfile /></Route>"
		],
		"description": "The `useRouteMatch` hook attempts to match the current URL in the same way that a <Route> would. It’s mostly useful for getting access to the match data without actually rendering a <Route>."
	}
}
