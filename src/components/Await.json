{
	"import Router <Await>": {
		"prefix": ["imaw", "importAwait"],
		"body": ["import { Await } from 'react-router'"]
	},
	"Router <Await>": {
		"prefix": ["Await"],
		"body": [
			"<Await resolve={${1:promise}}>",
			"\t{(${2:data}) => <div>${3:{data}}</div>}",
			"</Await>"
		]
	}
}
