{
	"import Router <Navigate>": {
		"prefix": ["imrn"],
		"body": [
			"import { Navigate } from 'react-router-dom'",
			"${0:{condition && <Navigate to={path\\} replace={true\\} />\\}}"
		],
		"description": "import Router `<Navigate>`\nA `<Navigate>` element changes the current location when it is rendered.\n`<Navigate>` instead of `<Redirect>`\n"
	},

	"Router <Navigate>": {
		"prefix": ["Navigate"],
		"body": ["<Navigate to=\"${1:/path}\" replace={true} />"],
		"description": "A `<Navigate>` element changes the current location when it is rendered. It's a component wrapper around useNavigate, and accepts all the same arguments as props.\n"
	}
}
