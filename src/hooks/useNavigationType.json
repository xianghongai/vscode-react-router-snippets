{
	"import useNavigationType": {
		"prefix": ["imunt", "importUseNavigationType"],
		"body": ["import { useNavigationType } from 'react-router'"]
	},
	"useNavigationType()": {
		"prefix": ["useNavigationType"],
		"body": [
			"const ${1:navigationType} = useNavigationType()",
			"// ${1:navigationType}: 'POP' | 'PUSH' | 'REPLACE'"
		]
	}
}
