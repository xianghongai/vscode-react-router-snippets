{
	"import useParams": {
		"prefix": ["imrup"],
		"body": [
			"import { useParams } from 'react-router-dom'",
			"${0:let { param \\} = useParams()}"
		],
		"description": "import `useParams` hook. \n"
	},

	"Router useParams": {
		"prefix": ["useParams"],
		"body": ["const { ${1:slug} } = useParams()"],
		"description": "useParams returns an object of key/value pairs of URL parameters. Use it to access match.params of the current `<Route>`.\n"
	}
}
