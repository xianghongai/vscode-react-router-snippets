{
	"import useMatch": {
		"prefix": ["imum", "importUseMatch"],
		"body": ["import { useMatch } from 'react-router'"]
	},
	"useMatch()": {
		"prefix": ["useMatch"],
		"body": ["const ${1:match} = useMatch('${2:path}')"]
	}
}
