{
	"import useSearchParams": {
		"prefix": ["imusp", "importUseSearchParams"],
		"body": ["import { useSearchParams } from 'react-router'"]
	},
	"useSearchParams()": {
		"prefix": ["useSearchParams"],
		"body": [
			"const [${1:searchParams}, ${2:setSearchParams}] = useSearchParams()",
			"// ${1:searchParams}.get('${3:key}')",
			"// ${2:setSearchParams}({ ${3:key}: '${4:value}' })"
		]
	}
}
